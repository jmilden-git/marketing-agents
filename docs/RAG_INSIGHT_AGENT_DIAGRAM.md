# RAG Campaign Insight Agent Diagram (Placeholder)

```
Query -> Retriever (campaign_history.json + kpi_dictionary.yaml) -> Context Builder -> LLM Prompt -> Insight Summary
```

Swap in a proper architecture diagram when ready.
